# Optional. Leave unset on Vercel to use built-in `/api` → Render proxy (see vercel.json).
# For local dev, point at your Django API (default in code is http://localhost:8000/api).
# VITE_API_URL=https://your-service.onrender.com/api
